<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NMedia" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NMedia" parent="Base.Theme.NMedia"/>

    <style name="Theme.NMedia.LikeButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/like_color</item>
        <item name="android:maxWidth">@dimen/std_icon_size</item>
        <item name="android:maxHeight">@dimen/std_icon_size</item>
        <item name="android:textSize">@dimen/std_btn_text_size</item>
        <item name="iconEndPadding">0dp</item>
    </style>
    <style name="Theme.NMedia.ShareButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="android:textColor">?attr/colorControlNormal</item>
        <item name="android:color">?attr/colorControlNormal</item>
        <item name="android:maxWidth">@dimen/std_icon_size</item>
        <item name="android:maxHeight">@dimen/std_icon_size</item>
        <item name="android:textSize">@dimen/std_btn_text_size</item>
        <item name="iconEndPadding">0dp</item>
    </style>

</resources>
